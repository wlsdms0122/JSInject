# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'JSInject' do
    # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!

    # Pods for JSInject

    target 'JSInjectTests' do
        inherit! :search_paths
        # Pods for testing
    end
end

target 'JSInjectDemo' do
    # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!

    # Pods for JSInjectDemo
    # RxSwift
    pod 'RxSwift', '~> 5'
    pod 'RxCocoa', '~> 5'
    # Layout
    pod 'SnapKit', '~> 5.0.0'

    target 'JSInjectDemoTests' do
        inherit! :search_paths
        # Pods for testing
    end
end
